# Work Log 2025-11-10

## Session 1: Initial Setup
- Initialized graft proposal starter.

## Session 2: Slice 0 Refactoring & Architecture Setup

### Completed Tasks

#### 1. Environment Setup
- Installed Python 3.14 using `uv python install 3.14`
- Created virtual environment with `uv venv --python 3.14`
- Installed dependencies with `uv pip install -e ".[test]"`
- All tests now run successfully on Python 3.14

#### 2. Bug Fixes
- **Fixed indentation error in `cli.py`**: The entire file had 4-space leading indentation causing `IndentationError`
- **Added `__main__` block**: Enabled CLI to run via `python -m graft.cli` for testing
- **Test verification**: `tests/test_explain_json.py` now passes

#### 3. Architecture Refactoring: Layered Design

Refactored Slice 0 (`explain` command) to use clean architecture with proper separation of concerns:

**Domain Layer** (`src/graft/domain/`):
- Created `entities.py` with core domain entities:
  - `Artifact` - Main domain entity representing a graft artifact
  - `GraftConfig` - Complete graft.yaml configuration
  - `Derivation` - Single derivation specification
  - `Inputs`, `Material`, `Output`, `Template`, `Policy` - Value objects

**Adapter Layer** (`src/graft/adapters/`):
- Created `filesystem.py`:
  - `FileSystemPort` protocol defining file I/O interface
  - `LocalFileSystem` implementation for local file operations
- Created `config.py`:
  - `ConfigAdapter` for loading and parsing `graft.yaml` files
  - Converts raw YAML to domain entities

**Service Layer** (`src/graft/services/`):
- Created `explain.py`:
  - `ExplainService` orchestrating artifact explanation use case
  - `ExplainResult` result object with `.to_dict()` for serialization
  - Clean separation from presentation concerns

**CLI Layer** (`src/graft/cli.py`):
- Refactored `explain` command to delegate to `ExplainService`
- Dependency injection: wires up adapters and services at module level
- Thin presentation layer handling output formatting and error messages

#### 4. Architecture Decision Records (ADRs)

Initialized `docs/adr/` directory with two ADRs:

**ADR 0001: Record Architecture Decisions**
- Standard template explaining what ADRs are
- Establishes ADR format and process
- Documents why we use ADRs

**ADR 0002: Layered Architecture with Separation of Concerns**
- Documents the four-layer architecture (Domain, Adapter, Service, CLI)
- Explains dependency flow and design principles
- Describes benefits and trade-offs
- Provides implementation guidance

### Test Results

All 5 tests passing:
```
tests/test_dvc_scaffold.py .
tests/test_explain_json.py .
tests/test_impact_and_simulate_stubs.py .
tests/test_run_stub.py .
tests/test_status_and_finalize.py .
```

### Key Decisions

1. **Immutable domain entities**: Used `@dataclass(frozen=True)` for value objects to prevent accidental mutation
2. **Protocol-based ports**: Used `typing.Protocol` for adapter interfaces to enable static type checking without runtime overhead
3. **Explicit dependency injection**: Services receive dependencies via constructor rather than global state
4. **Result objects**: Services return domain-specific result objects with serialization methods

### Files Modified

- `src/graft/cli.py` - Fixed indentation, added `__main__`, refactored `explain` command

### Files Created

- `src/graft/domain/entities.py`
- `src/graft/adapters/filesystem.py`
- `src/graft/adapters/config.py`
- `src/graft/services/explain.py`
- `docs/adr/0001-record-architecture-decisions.md`
- `docs/adr/0002-layered-architecture-with-separation-of-concerns.md`

### Next Steps

Slice 0 is complete with proper architecture in place. Future slices should follow this pattern:

1. **Slice 1**: Implement `run` command using same layered approach
   - Create `RunService` in services layer
   - Reuse existing adapters where applicable
   - Add template rendering capabilities

2. **Slice 2**: Implement `status`, `validate`, `finalize` commands
   - Create `StatusService`, `ValidateService`, `FinalizeService`
   - Add provenance tracking capabilities

3. **Subsequent slices**: Follow ADR-0002 architecture for all new features

### Notes

- The layered architecture adds initial overhead but provides strong foundation for maintainability
- Each layer is independently testable
- Pattern is agent-friendly with clear boundaries and responsibilities
- All changes maintain backward compatibility with existing tests

## Session 3: Slice 1 Implementation - Deterministic Single Derivation

### Completed Tasks

#### 1. Comprehensive Test Suite (tests/test_run_stub.py)

Wrote 8 black-box integration tests covering:
- Basic Jinja2 template rendering to output files
- --id flag for targeting specific derivations
- Missing template file error handling (exit code 1)
- Invalid artifact path error handling (exit code 1)
- Automatic parent directory creation for nested outputs
- Multiple outputs per derivation
- Processing all derivations when no --id specified
- Compatibility with existing sprint-brief example

**Test Philosophy**: All tests are black-box subprocess tests via `python -m graft.cli`. Tests verify CLI contract, exit codes, and file outputs—no internal imports.

#### 2. Service Layer Implementation (src/graft/services/run.py)

Created `RunService` following layered architecture pattern:

**RunService class**:
- Takes `ConfigAdapter` and `FileSystemPort` via dependency injection
- `run(artifact_path, derivation_id=None)` method for executing derivations
- Returns `RunResult` with execution details

**Key features**:
- Jinja2 template rendering with empty context (Slice 1 scope)
- Support for --id flag to filter specific derivations
- Proper error handling with custom exceptions:
  - `TemplateNotFoundError` for missing template files
  - `TemplateRenderError` for Jinja2 rendering failures
- Creates parent directories automatically for nested outputs
- Handles multiple outputs per derivation

**Design decisions**:
- Slice 1: Renders templates with empty context (no variable substitution)
- Future slices will add context loading from materials/inputs
- Focused on deterministic template-to-output transformation

#### 3. CLI Integration (src/graft/cli.py)

Refactored `run` command to use `RunService`:
- Replaced procedural stub with service delegation
- Added comprehensive error handling per exit code contract:
  - Exit 0: Success
  - Exit 1: User errors (missing template, invalid path, YAML errors)
  - Exit 2: System errors (permissions, unexpected exceptions)
- Human-readable output: "Run complete: N derivation(s), M output(s)"

#### 4. Dependency Management

- Added Jinja2 >=3.1.0 to `pyproject.toml` dependencies
- Installed via `uv pip install jinja2`
- Updated example to use `engine: jinja2` (was `mustache`)

#### 5. Files Modified

**Modified**:
- `tests/test_run_stub.py` - Expanded from 1 test to 8 comprehensive tests
- `src/graft/cli.py` - Refactored run command to use RunService
- `pyproject.toml` - Added Jinja2 dependency
- `examples/agile-ops/artifacts/sprint-brief/graft.yaml` - Changed engine to jinja2

**Created**:
- `src/graft/services/run.py` - New RunService with Jinja2 rendering

### Test Results

All 23 tests passing:
```
tests/test_dvc_scaffold.py .                  [  4%]
tests/test_explain_json.py ............       [ 56%]
tests/test_impact_and_simulate_stubs.py .     [ 60%]
tests/test_run_stub.py ........                [ 95%]
tests/test_status_and_finalize.py .           [100%]

============================== 23 passed in 2.42s
```

### Slice 1 Acceptance Criteria ✅

All acceptance criteria met:
- ✅ `run` writes outputs by rendering templates
- ✅ `run --id` targets specific derivation
- ✅ Missing template file → exit code 1 with error message
- ✅ Exit status 0 for success
- ✅ All tests in `tests/test_run_stub.py` passing

### Architecture Compliance

Slice 1 follows the layered architecture from ADR-0002:
- **Domain Layer**: Reused existing entities (Artifact, Derivation, etc.)
- **Adapter Layer**: Reused ConfigAdapter and FileSystemPort
- **Service Layer**: New RunService with clear business logic
- **CLI Layer**: Thin presentation layer with dependency injection

### Key Implementation Details

1. **Template Engine**: Jinja2 chosen as default and only supported engine
2. **Context Handling**: Empty context for Slice 1; future slices will load materials
3. **Error Propagation**: Service raises specific exceptions, CLI converts to exit codes
4. **File Operations**: Delegated to FileSystemPort for testability

### Next Steps

Slice 1 complete. Ready for Slice 2:
- Implement `status`, `validate`, `finalize` commands
- Add change origin classification (authored vs generated)
- Implement provenance tracking with `.graft/provenance/finalize.json`

### Notes

- Jinja2 integration seamless with no rendering errors
- Empty context rendering works well for static templates
- All existing tests continue to pass (backward compatibility maintained)
- Test suite runs in <3 seconds, excellent for TDD workflow
